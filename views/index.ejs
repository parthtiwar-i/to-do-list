<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <link rel="icon" href="images/notes.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />

    <title>Notes-ToDoList</title>
  </head>
  <body>
    <div class="container1 bg-green-300 text-center min-h-[700px]">
      <div class="quotes py-2 text-[#2f4f4f]">
        <p class="randomQuotes text-center p-2 text-xs md:text-lg">
          " <em class="px-10"><%= quotesRandom %></em> "
        </p>
      </div>

      <div
        class="weather p-2 text-sm text-center md:text-base md:mt-10 md:ml-8 md:fixed md:top-56 md:w-64"
      >
        <h1 class="text-[#2f4f4f] inline p-2">
         <em> The temprature in <%= city %> is <%= temprature %> deg celcius </em>
        </h1>
        <img class="mx-auto w-2" src="<%= icon %>" alt="" />
      </div>

      <div class="Notes py-5 text-[#2f4f4f]">
        <div class="mainHeading flex justify-center items-center my-5">
          <img class="w-16 md:w-24" src="images/notes.png" alt="" />
          <h1 class="text-2xl md:text-4xl md:px-10 font-extrabold">
            <%= Heading %>
          </h1>
        </div>
        <div class="mainHeading flex justify-center items-center mb-4">
          <h1 class="text-2xl md:text-2xl md:px-10 font-extrabold">
            <%= currentDate %>
          </h1>
        </div>
        <div
          class="toDoList mx-8 md:w-[40%] md:mx-[auto] shadow-2xl border-[1px] rounded-md bg-[wheat]"
        >
          <div class="listContent text-lg text-black text-start py-7">
            <div class="listItems py-2 px-4 leading-10 flex flex-col">
              <!-- here is the list content -->
              <% listItems.forEach((item)=>{%>
                <form action="/delete" method="post">
               <div class="items"><input type="checkbox" name="onCheck" value="<%= item._id %>" onChange="this.form.submit()"> <p class=" inline pl-2 md:pl-5 text-sm md:text-base"> <%=item.name%>  </p>  </div>  
              <input type="hidden" name="listName" value="<%=Heading%>">
              </form>
               <%  }) %>

            </div>
            <div class="inputItems text-center py-2 md:px-3">
              <form action="/" method="post">
                <input
                name="newItem"
                class="text-center"
                id="userInputValue"
                type="text"
                autocomplete="off"
                placeholder="Add Item"
              />
              <button
              name="list"
              value="<%=Heading%>"
                id="addButton"
                class="md:ml-14 mx-auto mt-3 block sm:inline smallButton md:addButton"
              >
                +
              </button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
    <script src="/script.js"></script>
  </body>
</html>
